server:
  port: 3344

spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/szwei/microservicecloud-config.git    # ssh 方式不知道怎么回事不能用
