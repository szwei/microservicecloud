spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client  # 需要从GitHub上读取的资源名称，没有yml后缀
      profile: test
      label: master
      uri: http://localhost:3344  # SpringCloudConfig获取的地址